{
  "name": "edgescribe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler pages deploy dist",
    "build": "wrangler pages functions build --outdir dist --sourcemap"
  }
}
